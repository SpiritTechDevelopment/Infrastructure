# Keep operator-side syntax and execution semantics identical to the protected
# management executor installed by roles/platform_executor.
ansible-core>=2.18,<2.19
ansible-lint>=26,<27
yamllint>=1.35,<2
PyYAML>=6,<7
jsonschema>=4.18,<5
# Резолвер выводит публичный ключ REALITY из приватного, чтобы сверить пару с
# объявленной в топологии. `ansible-core` тянет ту же библиотеку транзитивно,
# но полагаться на это нельзя: смена его зависимостей уронила бы сверку — а
# сверка молчит ровно тогда, когда всё в порядке, и её исчезновение незаметно.
cryptography>=41
