# Legacy design documents

These files describe the abandoned hardening/private-management design. They are retained
only as historical context. They are **not** executable deployment instructions.

The active repository contract is documented in the root `README.md`: complete public
runtime, public Xray gRPC API, public telemetry ingestion, and no SSH/firewall/Fail2ban/
WireGuard changes.
