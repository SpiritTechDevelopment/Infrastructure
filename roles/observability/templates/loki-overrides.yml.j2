# Per-tenant retention (spec §16.2): activity carries the shortest viable retention.
overrides:
  ops:      { retention_period: {{ retention_ops }} }
  activity: { retention_period: {{ retention_activity }} }
